# Tools

Small, focused scripts for observing sources, preserving raw evidence, and searching the wiki.

- `tools/ingestion/` — source-specific fetchers.
- `tools/search/` — local BM25 and optional vector search over `wiki/`.
